import React, { Component } from 'react'
import Divider from 'material-ui/Divider'

// Useful for when you need to give a Divider a ref
// for example, when using FlipMove
export default class StatefulDivider extends Component {
  render () {
    return (
      <Divider />
    )
  }
}
